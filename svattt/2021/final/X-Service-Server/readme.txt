-- install package:
pip install flask
pip install flask-socketio

-- flask key
export KEY='md5(random(uuid4()))' moi~ lan` chay. docker

-- flag
filename: flag_<random-md5>
content: ASCIS2021{random} moi~ lan` chay. docker o? /

-- source in src folder